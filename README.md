# Telegram Bot - Магазин с криптоплатежами

Telegram бот для продажи товаров с поддержкой криптовалютных платежей и админ-панелью.

## Возможности

- 🏙️ Выбор города и просмотр товаров
- 📦 Детальная информация о товарах
- 💳 Оплата через криптовалюту (BTC, ETH, TRC, USDT)
- 🔐 Админ-панель для управления городами, товарами и методами оплаты
- 💾 SQLite база данных для хранения данных

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

3. Заполните переменные окружения в `.env`:
```
BOT_TOKEN=your_bot_token_here
ADMIN_PASSWORD=password123
DB_PATH=./database/bot.db
```

4. Запустите бота:
```bash
npm start
```

Для разработки с автоперезагрузкой:
```bash
npm run dev
```

## Использование

### Для пользователей

1. Откройте бота в Telegram и отправьте `/start`
2. Выберите город из списка
3. Выберите товар
4. Выберите метод оплаты
5. Получите адрес для оплаты

### Для администраторов

1. Отправьте команду `/apanel password123` (замените `password123` на ваш пароль из `.env`)
2. Используйте меню для управления:
   - **Города**: добавление и удаление городов
   - **Товары**: добавление и удаление товаров в городах
   - **Методы оплаты**: управление методами оплаты и реквизитами

### Команды админ-панели

- `/addcity Название города` - добавить город
- `/addproduct cityId Название|Описание|Цена` - добавить товар
- `/addpayment Название|Сеть` - добавить метод оплаты
- `/setaddress methodId Адрес` - изменить адрес для метода оплаты

## Структура проекта

```
telegram-bot/
├── src/
│   ├── config/          # Конфигурация
│   ├── database/        # Работа с БД
│   ├── handlers/        # Обработчики команд и действий
│   ├── services/        # Бизнес-логика
│   ├── utils/           # Утилиты
│   └── index.js         # Точка входа
├── database/            # Файлы БД SQLite
├── .env                 # Переменные окружения
└── package.json
```

## База данных

Используется SQLite с следующими таблицами:
- `cities` - города
- `products` - товары
- `payment_methods` - методы оплаты
- `payment_addresses` - реквизиты для оплаты

При первом запуске автоматически создаются моковые данные для тестирования.

## Технологии

- **Telegraf** - фреймворк для Telegram ботов
- **SQLite3** - база данных
- **dotenv** - управление переменными окружения

